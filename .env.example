# PostgreSQL
POSTGRES_USER=intesa
POSTGRES_PASSWORD=intesa123

# JWT
SECRET_KEY=your-super-secret-key-change-in-production
SESSION_SECRET_KEY=your-strong-session-secret

# OpenAI
OPENAI_API_KEY=

# Google OAuth (svc-auth)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FRONTEND_URL=http://localhost:80

# Default admin credentials (svc-auth seeder)
DEFAULT_ADMIN_EMAIL=admin@yourcompany.com
DEFAULT_ADMIN_PASSWORD=securepass

# Email — MailHog (local dev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=noreply@orbis.io
EMAIL_ENABLED=True

# Email providers (svc-admin / svc-auth — configured via admin panel)
SENDGRID_API_KEY=

# Twilio SMS (svc-auth OTP)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Tavily (web search)
TAVILY_API_KEY=

# GitHub (workflow search nodes)
GITHUB_TOKEN=

# Internal service-to-service key
INTERNAL_API_KEY=intesa-internal-key-dev

# Vite (UI build)
VITE_API_BASE_URL=http://localhost:80
